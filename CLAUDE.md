# 포트폴리오 통합 웹사이트 — 작업 브리프 v3

> **상태: 통합 완료 (2026-07-15).** v1(빈 저장소 가정)·v2(통합 계획)는 폐기.
> 사이트 구조·유지보수 방법은 [README.md](README.md)에 있다. **이 문서는 남은 일과 판단 근거만 기록한다.**
> 아직 커밋하지 않았다. 배포는 사용자 확인 후.

---

## 1. 남은 일 (다음 세션의 작업)

> **[REDESIGN.md](REDESIGN.md) 구현 완료 (2026-07-16).** 3D 캐러셀 히어로 · 목차 바 · 섹션 재배치 전부 반영·검증됨.
> 캐러셀 구조와 함정 3가지는 [README.md](README.md) "index의 3D 캐러셀"에 있다. REDESIGN.md는 이제 기획 이력 문서다.
> 구현 중 지시서와 달라진 2건은 아래 §2-6 참조.
>
> **헤스티 페이지 교체 (2026-07-17).** 사용자가 새로 작업한 헤스티 원본으로 옛 `hesti-character.html`을 내용 교체(파일명은 짧은 슬러그 유지).
> 새 파일은 base64 인라인 원본이라 5개와 같은 파이프라인을 태웠다 — 이미지 33개 추출, 토큰 최신 정렬(원쁠원식 별칭),
> Pretendard static, 셸 주입, 캐러셀/썸네일 재크롭. 상세는 §2-8.
>
> **이력서 채움 (2026-07-17).** 사용자의 노션 포트폴리오(Hyun Moment = buly.kr/3NK26CP)에서 학력·경력·활동·수상·기술을 가져와
> `#resume`의 `{{...}}` 4칸을 **대표 항목 선별**로 채웠다. 연락처는 사용자 지시로 **전화번호 제거, 이메일+SNS만**. 상세는 §2-9.
>
> **Cinematic Dark 리뉴얼 — 전체 완료 (2026-07-17).** [DESIGN_RENEWAL.md](DESIGN_RENEWAL.md) 지시서대로 **index + 프로젝트 5개 전부 다크**.
> 처음엔 "index만"이었으나 사용자가 "기획단대로 모두" 지시 → 프로젝트 5개도 다크 전환(각 `:root` 토큰 다크 매핑 + 하드코딩 보정). 상세는 §2-10.

> **이력 섹션 리뉴얼 완료 (2026-07-18).** [RESUME_RENEWAL.md](RESUME_RENEWAL.md) 구현. 노션 대비 "약하다" 진단 → 날짜칩 컬럼·스킬 레벨바 22종·수상 2열 그리드·게임엔진 검증배지로 재구성. 노션 전량(활동 14·자격증 4·스킬 22) 담음. 상세는 §2-11.
>
> **7건 일괄 개편 (2026-07-19).** ① 이력서를 **벤토 패널 그리드**로 재디자인 + 노션 누락분(개인 연구 7·IT 서비스 4·수상 출처) 보강,
> ② 영역명 어미 변경(창작 게임 기획서/역기획서/제안서), ③ **분석·리뷰 섹션 신설**(스토브 리뷰 2편 + 유튜브 임베드),
> ④ 연락처 이메일만, ⑤ 다른 기록 문구 교체, ⑥ 히어로 헤드라인 교체. 상세는 §2-12.

| # | 할 일 | 위치 | 비고 |
|---|---|---|---|
| 1 | 카드 문안 검수 | `index.html` `.cards` | 각 페이지 원문에서 뽑아 작성함. 사용자 검수 필요(선택) |
| 1-b | 히어로·리뷰·유튜브 문안 검수 | `index.html` `.cr-kick` / `#analysis` | 2026-07-19 세션이 작성(헤드라인·리뷰 요약·채널 소개). 사용자 검수 필요(선택) |
| 1-c | 스탯 타일 숫자 검수 | `index.html` `.rz-stats` | 기획 문서 5 · 팀 게임 제작 4 · 수상 4 · 라이브 서비스 1 — 노션 근거. 틀리면 숫자만 고치면 됨 |
| 1-d | ~~신규 영역 3종 구현~~ **완료(2026-07-19)** | index.html `#tension`·`#unreal`·`#play` | ① 언리얼 레벨, ② 긴장 곡선 부록, ③ 지금 하는 게임 전부 구현·검증 완료(§2-15). 기획 이력은 [NEW_SECTIONS_PLAN.md](NEW_SECTIONS_PLAN.md) v3. 비차단 검수 4건(평면도 범례·원작 맵명/UE5 표기·경쟁 게임 티어·맵 카피)은 원할 때 한 줄 수정 |
| 1-e | ~~페이지 IA 재배치 구현~~ **완료(2026-07-20)** | [PAGE_FLOW_PLAN.md](PAGE_FLOW_PLAN.md) | A안 확정 → 구현·검증 완료. 동시 요청 5건(히어로 3문장 회전·리뷰/유튜브 분리·플레이 데이터표·이력서 lede 제거·수치)도 반영. 상세 §2-16 |
| 1-f | ~~루시 테마~~ **적용 후 원복(2026-07-20)** | — | 무드 안 어울린다는 판단으로 `theme-lucy.css` 삭제 + link 제거 → 앰버 복귀. **스위치 구조 실증**(파일 삭제=복원). 재적용 레시피는 THEME_LUCY_PLAN §7. 상세 §2-17 |
| 1-g | 스킬 로고 이미지 — **레벨바 유지로 결론(2026-07-20)** | — | 노션 로고 추출을 두 경로로 시도했으나 **인앱 브라우저=노션 SPA 5분 행 / 사용자 Chrome 확장=미연결(빈 목록)**. 사용자가 "불가능하면 레벨바 유지" 지시 → **현행 유지**. 나중에 파일(또는 노션 이미지 URL) 제공 시 `assets/img/skill-logos/`로 드롭인 가능 |
| 2 | 배포 | — | `git add -A && git commit && git push origin main` → 1~2분 후 https://00ny.github.io. **push는 사용자 환경에서만 가능**(세션에 GitHub 인증 없음 — 전례: 커밋까지 하고 push는 사용자가 함) |

### 선택 개선 (사용자 판단 필요)

- **accent 대비 미달 2건.** 원쁠원 `#2E8B47`(3.99:1), 가마솥 `#B06A28`(3.96:1)이 WCAG AA(4.5:1) 미달.
  기존 페이지의 정체성 색이라 유지했다. 본문 텍스트엔 안 쓰이고 라벨·아이브로우에만 쓰인다.
  고치려면 각 페이지 `:root`의 `--accent` 한 줄. (여우숲 5.70 / 엔드필드 5.31 / 헤스티 4.45는 통과)
- **원쁠원 판독 폭 880px** — 나머지 4개는 1120px. 완성된 페이지의 레이아웃 결정이라 건드리지 않았다.
  통일하려면 `--maxw`와 `.inner`/`.row`/`.hero-inner`의 `max-width`를 함께 고쳐야 한다(테이블·갤러리 재검증 필요).
- 원쁠원에 `<meta name="description">`이 없다. 5개 중 유일. SEO·공유 시 아쉬움.

## 2. 이번에 내린 판단과 근거 (되돌리기 전에 읽을 것)

### 2-1. "최신 세대"의 정의

토큰 스키마로 세대가 갈렸다. 측정 결과:

- **최신 = 역기획 3종**(헤스티·엔드필드·가마솥). `--font-title/body/mono`, `--slate`, `--observed`, `--patch-soft`, `--maxw` 스키마가 완전 동일. 색 역할까지 주석으로 명세.
- **중간 = 원쁠원.** 팔레트 값은 최신과 동일한데 변수명만 구식(`--disp/--body/--mono`).
- **구세대 = 여우숲.** 다크 테마(#080b11) + Noto 폰트. Pretendard·Hahmlet 미사용.

→ 여우숲은 **가장 오래된** 페이지였다. 최신 3종의 시스템으로 끌어올렸다.

### 2-2. 여우숲 — 히어로의 어둠은 살렸다

최신 세대는 **어두운 히어로(slate) → 밝은 본문 → 어두운 푸터** 패턴이다.
여우숲의 다크 히어로는 이 패턴과 충돌하지 않고 오히려 부합하므로, 히어로의 밤 연출·빛줄기는 유지하고 본문만 라이트로 전환했다.
"통일"은 다크를 전부 지우는 게 아니라 같은 시스템 문법을 쓰는 것이다.

### 2-3. 여우숲 accent = `#4A6B2A` 모스 그린

원래 앰버 `#F2B450`은 라이트 배경에서 **대비 1.71:1로 사용 불가**. 대비를 확보한 앰버(`#96661C`)는 가마솥 앰버와 **색상환 7° 차이**로 충돌.
색상환 빈 구역은 황록(60~110°)과 보라(240~280°)뿐이고 빨강·파랑은 시스템 예약색(patch/link)이다.
→ 모스 그린 선택. 게임 이름 그대로 '숲'에 묶이고, 원쁠원 켈리그린과 46° 이격(기존 가마솥↔헤스티는 2° 차이로 이미 붙어 있음), 대비 5.70:1.

### 2-4. 레거시 별칭을 남긴 이유

여우숲·원쁠원은 구세대 변수명이 **본문 인라인 `style`**에 박혀 있었다(여우숲 15곳). 본문을 건드리지 않기로 했으므로 `:root`에서 별칭으로 연결했다.
`--amber2:var(--accent)` / `--teal:var(--observed)` / `--mono:var(--font-mono)` 등.
**이 줄을 지우면 해당 요소 색이 깨진다.** 자세한 내용은 README의 "레거시 별칭".

### 2-5. 셸은 페이지 토큰을 상속한다

`assets/style.css`의 셸은 색·폭을 하드코딩하지 않고 `var(--accent)`, `var(--maxw)`를 쓴다.
→ 같은 마크업이 페이지마다 그 페이지의 색과 폭에 자동 정렬된다. 원쁠원(880px)과 나머지(1120px)가 각각 맞는 이유.
**셸에 색을 하드코딩하지 말 것.** 이 성질이 깨진다.

### 2-6. 캐러셀 구현 중 지시서(REDESIGN.md)와 달라진 2건

**① 가마솥 캐러셀 이미지 — 전용 크롭을 만들지 않고 `01.webp`를 그대로 썼다.**
지시서는 "01.webp는 세로 슬라이스 소스폭 253px로 부적합 → 02.webp의 기계를 크롭하라"였다. 실제로 해보니:
- 02.webp의 기계는 **머리·앞다리가 피라미드 이미지 블록(x 850~1065)과 겹쳐** 있어, 피라미드를 피해 자르면 머리 없는 기계가 된다.
- hzd 이미지 41장 중 게임 화면은 01뿐이고 나머지는 전부 문서 페이지(1500×1060 표·다이어그램)다.
- 01.webp를 레티나 표시 크기(544px)로 확대해 눈으로 확인한 결과, 삼각 구조물·푸른 게이트·하단의 에일로이가 살아 있고
  소프트함은 대형 구조물의 심도처럼 읽힌다. 지시서가 이미 허용한 여우숲(316px)과 같은 급이다.
→ **01.webp 채택.** `#projects` 카드와 같은 이미지라 인지도 일관된다.

**② 여우숲 캐러셀 이미지 — 지시서에 없던 크롭을 새로 만들었다(`foxforest/carousel.jpg`).**
지시서는 `01.jpg` + `object-position:center 55%`였는데, **그 55%는 아무 효과가 없다**(README 함정 2).
그 결과 타이틀 화면 전체 높이가 나와 **게임 메뉴 버튼이 카드 하단에 잘려** 고장처럼 보였다.
→ 좌측(밤하늘·나무 실루엣·빛나는 두 눈)을 2:3으로 잘라 `carousel.jpg` 생성. 공포 게임 카드로 더 적합하고 8KB로 가볍다.

그 외 구현 중 고친 것: 모바일 중앙 카드의 제목 중복(큰 타이틀과 카드 메타가 겹침), no-JS 폴백의 가로 스크롤,
터치 스와이프 신뢰성(`touch-action:pan-y` + `pointercancel` 처리).

### 2-7. 캐러셀 2차 보완 (2026-07-16, 사용자 지시)

- **카테고리를 제목급으로 승격.** `data-chip` 통합칩(11.5px) → `data-kind`+`data-field` 2개 칩(13px).
  성격(창작/역기획/제안)은 **흰 채움**, 분야(Unity 3D / MSW / 퀘스트·레벨·캐릭터)는 외곽선. 제목 바로 위.
- **설명이 안 보이던 문제 해결.** `#C4CCD4` 14.5px → **흰색 16.5px + 강한 그림자**.
  근본 원인은 색이 아니라 배경이었다 — 원쁠원의 밝은 노을 카드 위에서 회색이 묻혔다.
  → `.cr-title::before` **스크림**(라디얼 그라디언트)을 깔아 카드 밝기와 무관하게 읽히게 했다. **지우지 말 것.**
- **영역 분리 강화.** `.group` 하단 1px 연한 선 → **상단 2px `--ink` 굵은 선**, 여백 52→88px,
  제목 20px → `clamp(25px,3.2vw,32px)`, 편수는 알약 배지로.
- **순서 변경: 원쁠원을 여우숲 앞으로.** 순서는 3곳(캐러셀·카드·셸 체인)에 중복돼 있어 전부 고쳤다.
  셸은 `scratchpad/inject_shell.py`의 `PROJECTS` 배열을 고쳐 재주입했다(재실행 안전). 사이트 바 `01/05` 번호도 따라 바뀐다.

### 2-8. 헤스티 페이지 교체 (2026-07-17, 사용자 지시)

사용자가 헤스티를 새로 작업한 원본 `hesti_operator_proposal_baehyun.html`을 주며 옛 `hesti-character.html`을 대체·삭제하라 지시.
새 파일은 내용(제목·h1·이미지 33장 전부)이 바뀐 **base64 인라인 원본**이라, 5개와 같은 파이프라인을 태웠다:

- **이미지 33개 추출** → `assets/img/hesti-character/`에 덮어씀(옛 번호 이미지 먼저 삭제해 고아 방지). 새 이미지는 옛것과 0개 일치.
- **토큰 최신 정렬(원쁠원식).** 팔레트는 이미 최신인데 변수명만 구식(`--serif/--sans/--mono`, `--risk`, `--obs/--inf/--des`).
  최신 스키마를 추가하고 구식명은 레거시 별칭으로 연결(`--serif:var(--font-title)` 등). `--risk:var(--patch)`.
  **페이지 고유 3색 체계(--obs/--inf/--des = 관찰/추론/설계)와 --sage(히어로 밴드)는 고유값 유지** — 최신 --observed와 값이 다르다.
- **Pretendard Variable → static**(head link + --sans 별칭). style.css 링크 추가.
- **셸 주입**은 `inject_shell.py`의 5번째 슬러그를 새 파일명으로 바꿔 전체 재주입. 이때 **무릉의 '다음' 링크도 자동으로 새 파일명**이 된다.
- **캐러셀/썸네일 재크롭.** 새 히어로 캐릭터(01.webp, sage 배경 전신)로 `carousel.jpg`(2:3)·`thumb.jpg`(16:9) 재생성. sage 배경이라 캐러셀 배경 에코와 색이 맞는다.
- **index 참조 2곳**(캐러셀 카드·프로젝트 카드 href)을 새 파일명으로. 이미지 경로는 `hesti-character/` 유지라 그대로.

**파일명:** 사용자가 준 파일명은 `hesti_operator_proposal_baehyun.html`이었으나, 나머지 4개와 통일해 **`hesti-character.html`로 rename**(사용자 확인).
rename + 참조 3곳(index 캐러셀·카드, 무릉 next) 치환으로 처리. 이미지 폴더는 원래부터 `hesti-character/`라 무변경.

### 2-9. 이력서 채움 (2026-07-17, 사용자 지시)

사용자가 노션 포트폴리오 링크(`glowing-jackfruit-444.notion.site/Hyun-Moment-...` = 추가 링크의 buly.kr가 리다이렉트하는 곳)를 주며 참조를 요청.
Notion은 JS 렌더라 WebFetch로는 헤더만 나온다 — **Browser 도구로 열어 `get_page_text`**로 본문을 뽑았다(스크롤해야 하단 블록 로드됨).

- **대표 항목 선별**(사용자 선택). 노션엔 활동 15개+·스킬 20개+가 있으나 랜딩 이력서엔 과하므로 각 칸 핵심만.
- **연락처: 전화번호 제거, 이메일+SNS만**(사용자 선택). 노션 Contact에도 전화번호가 없다.
  SNS URL은 노션 `a[href]`에서 추출: 유튜브 `@videowoon`, 인스타 `daewae_hwaldong`, 블로그 `cl0udwoon`.
- 채운 4칸: 학력(숭실대·서울고) / 경력·교육(넥슨 메커톤·스마일게이트 스토브크루·NC AI Varco3D·겜마루 기획부장·자격증) /
  활동·수상(펄어비스 등 4개 수상·졸업전시 여우숲·유니데브·장학 3건) / 기술(Figma·Notion·Unity·MSW·Canva·CapCut·Midjourney 등 5카테고리).
- `lede`의 "{{...}} 안내" 문구는 "전체 이력은 추가 링크의 노션 포트폴리오에" 로 교체. buly.kr가 그 노션으로 리다이렉트됨을 curl로 확인.

**노션 참조 방법 메모:** 공개 Notion은 정적 HTML에 본문이 없다. `preview_start {url}` → `get_page_text`가 정답.
`javascript_tool`로 스크롤 시 무거워 타임아웃날 수 있다 — 그래도 `get_page_text`는 이미 로드된 블록을 가져온다.

### 2-10. Cinematic Dark 리뉴얼 (2026-07-17, DESIGN_RENEWAL.md)

사용자가 `DESIGN_RENEWAL.md`(다크 리뉴얼 지시서)를 주며 "그대로 리뉴얼" 지시. 착수 전 두 가지 확인:

- **문서 §7 가정이 틀렸다.** "토큰 한 곳 바꾸면 프로젝트 페이지도 자동 다크"라지만, 프로젝트 5개는 각자 `:root`에 `--paper:#F7F7F3`(라이트)를 소유해서 안 바뀐다. → 사용자에게 범위 확인. **"index만 먼저 다크"** 선택.
- **피처드작 = 원쁠원** (노션 대표작, 캐러셀 시작 카드).

핵심 판단·구현:
- **다크 토큰을 `:root`가 아니라 `.home`에 스코프.** `:root`(라이트)는 프로젝트 페이지 셸의 fallback이라 그대로 두고, `.home`에서 기존 토큰명(--paper/--ink/--card/--line/--slate/--muted/--accent)을 다크값으로 매핑 + 문서 신규명(--bg/--surface/--surface-2/--border/--text/--text-sub/--accent-strong)을 정의. → **index만 다크, 프로젝트 페이지 완전 무영향**(검증: 프로젝트 body 여전히 #F7F7F3). 컴포넌트 CSS는 대부분 그대로 두고 토큰만으로 다크 전환됨.
- **히어로 풀블리드.** cr-hero 100svh, 목차바 `position:fixed` 투명 오버레이 → 스크롤 시 `.is-scrolled`로 배경(JS 리스너). 스크림을 하단 `--bg` 수렴으로 강화해 다음 섹션과 끊김 없음(섹션은 투명, body 다크가 비침 → 흰 띠 0). 헤드라인 교체(`{{HERO_HEADLINE}}` 초안 + who 보조행, 페이드업 1회). 캐러셀 카드/타이틀 top 60%(헤드라인과 분리 + 인디케이터 간격). 빈 src 이미지(§8) 채움.
  **스크롤 인디케이터(2026-07-18 개편):** 카드에서 62px 떨어뜨림. 상시 표시가 아니라 `.cr-scroll.show` 토글 — 처음엔 보이고, 사용자 활동(mousemove/scroll/key/touch/wheel) 시 숨고, 10초 무활동 시 재등장(JS idle 타이머). 모바일 숨김.
- **카드.** 3영역(창작/역기획/제안) 유지 — 문서 §5-1 "피처드+서브4"와 사용자의 이전 3영역 지시가 충돌하나, **히어로 캐러셀=피처드 쇼케이스, 그리드=서브 목록**으로 해석해 양립. 넘버링 01–05(counter, 전체 연속), 썸네일 공통 다크 스크림+필터(§6), 칩을 캐러셀과 통일(성격=흰 채움/분야=외곽선 — 라이트 하드코딩 #EAEEF1이 다크에서 묻히던 것 수정).
- **액센트 절제(§3-1·3-2).** 섹션 아이브로우(.tag)·이력 라벨(.resume__k)을 엠버→text-sub로. 엠버는 히어로 강조어·영역마커·링크·hover에만.
- **푸터** --surface-2 마감. **이력서 이메일 링크**가 기본 파랑이던 것 `.home a{color:var(--link)}`로.

**엠버 최종 톤(§9-3) 미확정** — 시작값 `#E98A3C` 사용. 헤드라인 문안도 초안(사용자 확정 대기).

**프로젝트 5개 다크 전환 (2026-07-17, "기획단대로 모두" 지시).**
문서 §7은 "토큰 한 곳 바꾸면 프로젝트도 자동 다크"라지만 실제로는 각 페이지가 자기 `:root`에 라이트 토큰을 소유해서 **수동 전환**이 필요했다. index `.home` 스코프 방식과 달리, 프로젝트는 각 `:root` 값을 직접 다크로 재정의(본문 CSS는 var() 의존 89~135회라 대부분 자동 다크).

- **공통 다크 세트**(5개 동일): `--paper:#0E0F13 / --ink:rgba(255,255,255,.90) / --muted:.60 / --card:#16181E / --line:rgba(255,255,255,.09) / --slate:#20252E(표헤더·푸터) / --observed:.62 / --patch:#E8674E / --link:#7FB0E8`.
- **accent는 페이지별 밝은 버전**(라이트용 원본은 다크 배경에서 안 보임): 여우숲 `#A8CE7B` / 원쁠원 `#5BC77E` / 가마솥 `#E0A85C` / 무릉 `#52B4C6` / 헤스티 `#E89250`.
- **페이지 고유**: 여우숲 --night(히어로 밤), 원쁠원 히어로 노을(유지), 헤스티 --sage 밴드 `#ADC9A5→#33413A`(어둡게) + --obs/inf/des 3색 밝게.
- **하드코딩 보정 패턴**(공통): nav 라이트 유리 `rgba(247,247,243,.9x)→rgba(14,15,19,.85)`, 표 얼룩 `#FBFBF8→rgba(255,255,255,.025)`, notice/블록 라이트 배경 → 다크, 어두운 본문 텍스트(#3D4239 등) → 밝게. 원쁠원 표헤더 `color-mix(...,#fff)`(밝아짐) → `var(--slate)`.
- **자동 다크가 된 것**: color-mix(토큰 기반), 어두운 면 위 흰 텍스트(#fff/#EEF1F4 — 히어로·표헤더·버튼), 라이트박스 흰 버튼(다크 오버레이 위).
- 스크립트: `scratchpad/dark_roots.py`(:root 토큰), `dark_hardcode.py`(하드코딩) — 역기획 3종 일괄. 여우숲·원쁠원은 개별(스키마 차이).

### 2-11. 이력 섹션 리뉴얼 (2026-07-18, 노션 대비 진단)

사용자가 노션(Hyun Moment)의 학력·활동·수상·스킬 화면과 우리 `#resume`를 비교 — "가독성·시각 요소가 약하다", 이어서 "왜 최소한만 담았나".
진단: 문제는 다크 톤이 아니라 **컴포넌트 부재 + 내용 축약**. 불릿 텍스트를 노션식 구조물로 바꾸고, 노션 전량을 담았다.

- **날짜 칩 컬럼**(`.ritem`+`.rd`): 모든 항목이 mono 박스 칩으로 시작 → 왼쪽 세로 스캔 축(폭 118px 통일). 학력·자격증·활동·장학에 적용.
- **스킬 레벨바**(`.skill`+`.bar` 10칸): 22종 전부 카테고리 그룹(협업/문서/AI/영상/디자인/SNS) + 세그먼트 바(on 개수=Lv) + Lv 배지. 처음엔 협업만 풀고 축약했다가 사용자 지적으로 **전량 레벨바**로.
- **수상 2열 그리드**(`.awards`): 4건, 등급(최우수상 등)만 `--accent-strong` 강조.
- **하이라이트 3건**(`.ritem--hot`): 넥슨 메커톤·겜마루 기획부장·유니데브 대외협력부만 엠버(노션의 강조를 따름, 액센트 절제).
- **게임 엔진 검증 배지**(`.skill--proof`): Unity 3D·MSW는 자기평가 레벨 대신 "프로젝트로 검증 →"로 해당 프로젝트 페이지 링크. **노션에 없는 우리만의 강점**(근거가 클릭 거리).
- **볼륨**: 이전 "대표 선별"을 뒤집고 게임 활동 12 + IT 2 전부 노출(노션 대비 지적 반영).
- 마크업은 `scratchpad/build_resume.py`가 데이터 표 → HTML 생성(레벨바 `<i>`×10×22 수작업 불가). 런타임 JS 아님(no-JS에서 레벨바 유지). 데이터 수정 시 그 표만 고쳐 재생성.

### 2-12. 7건 일괄 개편 (2026-07-19, 사용자 지시)

**① 이력서 벤토 재디자인 + 노션 누락분 보강.** "원본(노션) 대비 가독성·시각 요소가 약하고 누락이 많다" 재지적.
§2-11의 원장(레이블 컬럼 `.resume__k/v`) 방식이 단조로움의 원인이라 판단 → **주제별 패널을 12컬럼 벤토 그리드(`.rz`)에 배치**로 전환.
부품(`.ritem`/`.rd`/`.rgroup`/`.skills`)은 유지, 원장 CSS(`.resume__*`)는 삭제.
- 배치: Profile(7)+학력·자격증(5) / 게임 활동(7)+우측 스택 IT·수상·장학(5, `last-child flex:1`로 높이 맞춤) / 스킬 풀폭(엔진 검증배지 → 협업~SNS 3열, 920px↓ 2열, 640px↓ 1열).
- Profile에 **스탯 타일 4개**(기획 문서 5 · 팀 게임 제작 4 · 수상 4 · 라이브 서비스 1 — 노션 Introduction·MSW 서비스 중 근거). 숫자는 흰색(액센트 절제 유지).
- 노션에서 추가로 가져온 것: **개인 연구·자습 7건**(엔드필드 간편제작 UI 역기획 Figma 프로토타입, 언리얼 FPS 레벨 2종, 오픈월드 맵(진행 중 `.ing` 칩), 니달리 분석, 메이플 아이템 버닝 분석, 퀘스트/캐릭터 자습, 유니티·마야 1인 개발) — 날짜가 없어 **날짜칩 자리에 태그**(Figma/Unreal/분석/자습/Unity)를 넣어 스캔 축 유지.
  **IT 서비스 4건 추가**(Albaid 숭실×중앙, SorTravel WISH 해커톤, PINGU AI-Pin, DR.DRIVE는 기존 예창패 항목에 병합). **수상 4건에 출처 설명**(`.award .for`) — 펄어비스=검은사막 모바일 크리에이터 활용 방안, 카카오=오픈채팅 이미지 개선안, 벨킨=마케팅 기획, WISH=SorTravel.
  **마케팅·영상 활동(제아페·젠틀몬스터 등)은 의도적으로 제외** — 사용자 지시가 "게임 집중 + IT 서비스 추가".
- 연락처: **이메일만**(블로그·유튜브·인스타 링크 삭제, 사용자 지시 ④). 푸터·#links의 블로그/노션 링크는 유지.

**② 영역명 어미.** 창작 게임→창작 게임 기획서 / 역기획→역기획서 / 제안→제안서. 적용 위치: 목차 바·`.group__title`·meta description·푸터 fine·JS 주석.
**칩(data-kind·chip--kind)은 "창작 기획서"**로 축약 — "창작 게임 기획서"는 모바일에서 field 칩과 한 줄에 안 들어간다.

**③ 분석·리뷰 섹션(#analysis) 신설 — 배치 기획의 결론.** 사용자가 "이력서 바로 다음 vs 캐러셀 승격" 중 기획을 요청.
**캐러셀 승격은 기각**: 캐러셀은 "완성 기획서 5편" 쇼케이스인데 외부 플랫폼 리뷰가 섞이면 프레임이 깨지고,
캐러셀의 조작 문법(중앙 클릭=내부 페이지 이동, 이전/다음 체인)과 외부 링크가 충돌한다.
대신 **이력서 바로 다음에 전용 섹션**: 좌(7fr) 스토브 리뷰 카드 2장(`.rvw` — 33원정대 QTE 실측 리뷰 / 디트로이트 분기 노드 85개 리뷰, 실측·역추적이라는 포트폴리오 정체성으로 문안 작성), 우(5fr) 유튜브 임베드(`.ytb`).
- 유튜브 임베드는 `youtube-nocookie.com/embed/OXLhJAlYUMI` iframe — GitHub Pages에서 문제없음. 채널은 **게임기획대학생(@endfield_univ)**(oEmbed로 확인). 이력서 스토브크루 항목에 `리뷰 2편 ↓` 앵커(`.rlink`) 연결.
- 주의: `.home a{color:var(--link)}`가 `.rvw`(a 태그) 내부 제목을 엠버로 물들인다 — `.rvw-t`에 `color:var(--text)` 명시로 해결(재발 주의).

**⑥ 히어로 헤드라인.** "잘 만든 게임을 문서로 뜯어보고,<br>뜯어낸 문법으로 <em>제 게임을 만듭니다</em>" —
역기획 2편·리뷰·유튜브(분석)와 창작 2편·MSW 라이브(제작)를 잇는 정체성 문장. 히어로-문구-바꾸기.md의 예시 블록도 동기화.

**리뷰 글 제목 "안생은 선택의 연속이다"의 '안생'은 오타가 아니라** 안드로이드+인생 말장난으로 판단(디트로이트 소재)해 그대로 옮겼다.

### 2-13. 이력서 워딩·마커·스킬 조정 (2026-07-19, 사용자 지시)

**워딩 5건.** ① 리뷰 CTA `리뷰 2편 ↓`→`게임 리뷰 보러가기 ↓`(#analysis 앵커 유지), ② 펄어비스·③ 카카오 수상 설명에 `기업 솔루션 :` 접두(마케팅인 벨킨은 미변경 — 성격이 다름), ④ 수상명 `WISH`→`WISH 해커톤`, ⑤ 그 설명 `「SorTravel」 해커톤`→`여행 후, 앨범 정리 도움 앱 서비스 기획`. IT 서비스 활동 행의 `「SorTravel」 WISH 해커톤`은 역할 정보라 미변경(지시가 수상 .for만 지목).

**색깔 마커 `.mk`(앰버 알약: bg `rgba(233,138,60,.16)` + accent-strong 텍스트 + `white-space:nowrap`).** 스마일게이트·NC AI 키워드 + 리뷰 CTA 3곳 한정. §2-10 액센트 절제 원칙을 **사용자가 브랜드 강조 목적으로 의도 완화** — 무한 확장 금지, 이 3곳만. `ritem--hot`(앰버 볼드 텍스트)과 시각적으로 구별됨(마커는 배경 알약). 스토브크루 행은 마커 2개(스마일게이트+CTA)라 다소 밀도 높으나 게임 리뷰의 출처라 의도적.

**스킬 패널: 3열→2열**(`.rz-skills .skills{repeat(2,1fr)}`, 920px 중간 규칙 제거 — 기본이 2열이라 불필요, 640px→1열 규칙은 유지). **Lv.10 6종에 `.skill--max`**(Notion·Figma·VLLO·Capcut·Canva·Instagram) → 바·배지 **노랑 `#F5C84B`**(앰버 accent와 구분되는 '만렙' 신호). CSS는 `.skill .bar i.on`과 특정도 동일(0,3,1)이라 **소스 순서로 승부** — `.skill--max` 규칙을 뒤에 배치해야 이긴다(재배치 금지).

**마크업은 index.html 직접 편집.** §2-11의 `build_resume.py`는 scratchpad라 소실됐다 — **이제 index.html이 스킬 마크업의 단일 소스**. Lv.10 추가/변경 시 해당 `.skill`에 `skill--max` 클래스를 손으로 붙인다.

**IT Service 정리 (2026-07-19 후속).** ① `「SorTravel」 WISH 해커톤` 행 삭제(아래 `WISH 해커톤` 수상과 중복 — 사용자 지시), ② 벤토에서 IT Service 카드는 데스크톱 폭 **437px**라 긴 설명이 2줄로 깨졌다 → 역할 꼬리(`— 기획 · 팀장` 등) 제거하고 **프로젝트명 + 맥락만**으로 축약해 전 행 1줄(JS 계측 lines:1 확인). ③ **Albaid·PINGU 날짜**는 사용자가 노션에서 확인해 줌(PINGU 24.03~06, Albaid 24.08~11 — 내가 처음 뽑은 노션 텍스트엔 안 딸려왔었음). 카테고리 칩(연합/교내)을 **날짜 칩으로 교체 + 카테고리는 텍스트 꼬리(`· 연합`/`· 교내`)로 이동**. 4행 데스크톱 1줄, 모바일 첫 행만 자연 줄바꿈(허용).

**영역명 '창작' → '프로젝트 기획서' (2026-07-19, 사용자 지시).** `창작 게임 기획서`/`창작 기획서`가 **"창작만 했다"로 읽힐 위험** → 전부 `프로젝트 기획서`로 교체. 위치: 캐러셀 `data-kind` 2곳·카드 `chip--kind` 2곳·목차·`group__title`·meta description·푸터 fine·JS 주석·여우숲 카드 alt(‘창작 게임 ’ 제거)·style.css 주석 2곳. **§2-12의 '창작 기획서로 축약' 결정은 폐기** — 신명칭 `프로젝트 기획서`(7자)도 모바일에서 field 칩과 한 줄 안 들어가면 flex-wrap으로 접힘(375px 가로 스크롤 0 확인). 역기획서·제안서는 불변.

**미반영 착시의 원인 = 미커밋.** 최근 두 세션 변경이 전부 미커밋(마지막 커밋 `ea0f850`)이라 배포 사이트엔 없다. 로컬에선 옛 `style.css` 캐시로 마커·노랑·2열이 안 보일 수 있음(하드 리로드). **push는 사용자 환경에서만** — 커밋은 사용자 요청 시에만.

### 2-14. 반영 착시 종결 + 스킬 열 확정 + 신규 3영역 작업 직전 준비 (2026-07-19)

**"여전히 3열" 목격의 진단.** 사용자가 3열·노랑 없음 스크린샷을 재보고 → 전수 감사 결과 **파일엔 `repeat(3` 규칙 자체가 없음**(2열+노랑 정상). 3열은 `ea0f850`(3열이던 시점) 배포본/브라우저 캐시다. 같은 착시가 3회 반복돼 **전례(커밋까지 하고 push는 사용자)에 따라 이번 세션에서 커밋 수행** — push만 하면 반영 끝.

**스킬 열 = 2열 확정(사용자가 "2열이든 4열이든 더 옳은 선택" 위임).** 근거: ① 레벨바가 정보 본체 — 4열이면 카드 ~250px, 바가 ~90px(칸당 6px)로 뭉개짐. ② 카테고리 크기(협업4·문서5·AI4·영상4·디자인2·SNS3)에서 낙오 행이 2열=2곳으로 최소(3열=4곳, 4열=빈칸 다수). ③ 엔진 검증 배지(2열)와 리듬 통일. ④ 640px↓ 1열과 반응형 연속.

**신규 3영역 작업 직전 준비(NEW_SECTIONS_PLAN.md v3).** ① 평면도↔맵 매칭을 **영상 썸네일 대조로 확정**(맵02 썸네일에 풀 블록아웃+미니맵 = `fps_수영장` 평면도와 일치, 맵01=`dungeon` 소거 확정. 보너스: 맵01 에디터에 `B_AI_Controller_Lyra…` → UE5 Lyra 기반+AI 봇 검증 확인). ② 에셋 스테이징: 평면도 2장 JPEG q85 **원본 해상도**(1차 시도가 1600px 업스케일이라 재작업 — 업스케일 금지), 긴장곡선 PDF p2~4를 **pymupdf**(poppler 부재 우회)로 160dpi 렌더→1800px, 원본 PDF는 `assets/doc/tension-3maps.pdf`. ③ 코드 초안(§7)까지 작성 — 구현은 붙여넣기+검증만 남음. 슬라이드가 라이트 배경인 건 프로젝트 페이지 원본 문서 발광체 문법(§2-10)과 동일해 허용.

### 2-15. 신규 3영역 구현 (2026-07-19, 사용자 "구현해")

NEW_SECTIONS_PLAN.md v3 §7 코드를 그대로 삽입. **파일 순서 = 서사 순서**(분석→설계→구현→평가 풀 사이클):

- **#tension (긴장 곡선 부록)** — `#projects` 내부, 제안서 카드 그룹 아래. `.group` 헤더 + `.tsn` 3열 카드(무릉 '실패'·가마솥 '훌륭'·여우숲 '아쉬움'). **"기획서 5편" 카운트 밖** — 넘버링 `.card` 대신 `.tsn-card` 신규 클래스(counter 미증가). 판정 워드 "실패"만 patch색. `.home`에 다크용 `--patch:#E8674E; --patch-soft:rgba(232,103,78,.16)` 토큰 추가(:root 라이트 patch와 별개). 카드가 `<a>`라 제목/설명에 `color:var(--text)`/`--muted` 명시(§2-12 `.home a` 앰버 누출 재발 방지). 슬라이드 이미지는 발광체(필터 없음).
- **#unreal (언리얼 레벨)** — `#projects`와 `#links` 사이 신규 섹션. `.umap` 카드 2장, 내부 `.umap-grid` 5:7(**평면도=설계 좌 → 영상=구현 우**, 읽는 순서=작업 순서). 임베드는 `.ytb-frame` 재사용(EQrjefizxbA, CrrPiAUJFuY). 칩은 `.chip--kind/field` 재사용(모작/창작 + 문법 학습/공간 번역).
- **#play (지금 하는 게임)** — `#links`와 푸터 사이, 맨 아래. `.play-grid` 4열 스탯 카드(트릭컬·니케·블아·명조) + "그리고" 행(경쟁 게임 티어 + 디트로이트·33원정대 `리뷰 ↑` #analysis 앵커 = 플레이→분석 순환 증명). 목차 미포함.
- **연결 4곳**: 이력서 엔진 배지에 `Unreal Engine`(→#unreal, 엔진 2열이라 3번째 반 폭 낙오 의도) · FPS 자습 행 `제작 기록 ↓` · 목차 `언리얼 레벨`(7번째, #play는 제외) · meta description 꼬리.

**검증(2026-07-19):** 신규 이미지 5장 전량 로드(naturalWidth 1800/1024/1129 확인)·iframe 3개(ID 정확)·엔진 배지 3개·목차 7개. 그리드 데스크톱(tension 3열·umap 419:587·play 4열)/모바일 375px(전부 1열, play 2열)·가로 스크롤 0. 판정색 실패=rgb(232,103,78)·훌륭/아쉬움 흰색·제목류 앰버 누출 0. 콘솔 0. **스크린샷 파이프라인은 비상단 위치에서 전부 검게 죽어 JS 계측으로 대체**(상단 캡처만 정상 — 목차 7개·캐러셀 '프로젝트 기획서' 육안 확인).

**#unreal 맵↔영상 매칭 교정 (2026-07-19, 사용자 지적).** §2-14의 "맵01=dungeon 소거 확정"은 **틀렸다** — 실제로는 언리얼 맵이 **3종**이었다. `plan-dungeon`(도심 항공)은 map01이 아니라 **진행 중인 세 번째 던전 맵**의 구상도였고, 영상1(EQrjefizxbA "유명 FPS 맵 그대로 옮겨보기")의 실제 설계물은 사용자가 새로 준 **MW_Shipment 블록아웃**(`plan-shipment.jpg`, `~/Downloads/언리얼에 사용_FPS 첫번째맵.png` 2560×1440→1600×900)이다. 교정 결과 **MAP 01=shipment+영상1(모작·블록아웃) / MAP 02=pool+영상2(창작) / MAP 03=dungeon+진행 중 플레이스홀더**(`.umap-wip` 신설, 영상 자리에 "블록아웃 제작 중"). 하단 브릿지 "01 옮기고 → 02 만들고 → 03 던전 도전 중"으로 3종화, 오픈월드 예고는 제거(맵03이 그 진행 중 역할 흡수). **교훈: 썸네일 소거법 매칭은 맵 개수 가정이 틀리면 통째로 무너진다 — 개수부터 사용자 확인.**

### 2-16. IA v2 구현 + 5건 동시 반영 (2026-07-20, 사용자 확정 "A안·권장·동의·승격")

**IA v2 (PAGE_FLOW_PLAN §5).** 최종 순서 `resume → projects(+tension) → unreal → analysis → youtube → skills → links → play`.
파이썬 수술로 블록 이동(레벨바 22종·리뷰 카드 2장은 재타이핑 없이 추출-재삽입, 어서션으로 개수 검증 — `<div class="skill`는 `skills` 래퍼도 매치하므로 카드 카운트는 `skill[ "]` 정규식으로). 이력서 스킬 패널은 **엔진 배지 3종만 남김**(rz-h `Game Engine — Proof by Projects`, 데스크톱 3열·900px↓ 1열). 목차 재배열(분석·리뷰가 언리얼 뒤). `#analysis`(리뷰)가 기존 id 승계 → 앵커 전부 유효.

**① 히어로 3문장 회전.** 마크업 기본 문장 = SENTS[0]("기획은 설득의 학문…") — no-JS·reduced-motion 폴백. JS가 3.4s 간격으로 s2("기획의도…")→s3("때문에, 항상 '왜'…배현입니다") 회전 후 **마지막 문장에서 정지**. 함정 2개를 밟고 고침: ⓐ 등장 애니메이션 `fill:forwards`를 해제하면 opacity가 베이스 0으로 추락 → 해제 직전 인라인 `opacity:1` 고정. ⓑ **rAF는 숨김 탭에서 지연 실행돼 마지막 opacity=1을 낡은 rAF(0)가 덮어쓰는 레이스**(탭 전환 사용자도 재현 가능) → rAF 대신 예약 순서가 보장되는 setTimeout(20ms)으로 페이드아웃 트리거. 브라우저 페인의 computed 0 판독은 숨김 탭에서 transition이 프레임 없이 멈춘 착시 — 프레임이 그려지면 1로 완주(스크린샷으로 증명). 편집 가이드는 히어로-문구-바꾸기.md 전면 개정(SENTS 배열 + 기본 문장 2곳).

**② 리뷰/유튜브 분리.** #analysis = 스토브 리뷰 전용(`.rvw-grid` 2열 — **리뷰가 늘면 카드만 복제**, 자동 개행), #youtube 신설(h2 "취미는 게임 분석 유튜브", `.ytb2` 영상 7 : 캡션 5). 구 `.anx`/`.ytb` 카드 CSS 삭제, `.ytb-frame`은 #unreal과 공용이라 유지.

**③ 플레이 데이터표.** `PLAY_GAMES` 배열 + `PLAY_ASOF`를 index.html 하단 스크립트로 분리, JS 렌더. **§2-11의 no-JS 원칙과 의도적 트레이드오프**(사용자의 상시 수정 요구 우선, 최하단 취미 코너라 허용 — no-JS에선 그리드만 빔). 엔드필드 카드 추가(LV.60 · `만렙 · 고인물` 칩 · "1.2ver 종결 콘텐츠 올클리어", 첫 자리 — 포트폴리오 소재 게임이라 의도 배치), 트릭컬 LV.39, ASOF 2026.07.20. `.play-grid`는 `auto-fit minmax(150px,1fr)`로 게임 수 증감 자동 대응. lede의 순환 문장 제거("게임을 좋아해서 시작한 일들입니다."만).

**④ 이력서 lede 제거**(깔끔한 이력서 — 사용자 지시). `.rz` margin-top 6→20px 보강. **⑤ 디자인 피드백은 채팅으로만 제공, 파일 변경 없음**(서체 3종·앰버 체계 유지 결론).

**검증(2026-07-20):** 섹션 순서 8개 정확·목차 7개 재배열·회전 s1→s2→s3 순서와 최종 가시성(스크린샷)·play 5카드(엔드필드 첫 자리·LV.39·ASOF)·이력서 바 0/엔진 3열·#skills 22바+노랑 6·rvw 2열·ytb2 610:436·모바일 375px 전부 1열(play 2열)·가로 스크롤 0·콘솔 0.

### 2-17. 루시 테마 + 히어로 넘김 컨트롤 (2026-07-20, 사용자 "구현해")

**루시 테마 (THEME_LUCY_PLAN).** `assets/theme-lucy.css` 단일 파일 + index `<link>` 1줄. **복귀 스위치**: 파일 삭제 → 남은 link는 404로 무해, 사이트가 #0E0F13 앰버로 완전 복원(검증됨). `.home` 스코프라 프로젝트 페이지 5개 무영향(각 `:root` --paper #0E0F13 불변 확인). 토큰 스왑(bg #0D0B1F 인디고·accent #FF3DA5 마젠타·text 라벤더화이트) + 하드코딩 10곳 재선언(캐러셀 스크림 4스톱·목차·칩 잉크·마커·엔진 배지). **Lv.10 노랑 → 애시드 그린 #52F55C**(엣지러너 로고 승계). patch는 네온 레드. L2 글로우 3곳은 파일 내 주석(주석 해제 시 켜짐). 대비 전수 통과(마젠타 링크 7.6:1·그린 12.7:1).

**히어로 넘김 컨트롤 (요청 1) — 최종: 화살표 양옆.** 자동재생이 마지막 문장에서 끝나면 화살표가 페이드인, 클릭 시 자동 중단(`interacted`) + 이전/다음(순환). `show(k)`가 §2-16 페이드(인라인 opacity+setTimeout 순서보장) 재사용. reset보다 "이전 문장 즉시 재열람"에 직접 부합해 arrows 채택.
- **1차 시도(폐기): 문장 아래 다크 글래스 알약 `‹ ●●● ›`(arrows+dots).** 사용자가 "3원과 화살표가 캐러셀을 침범한다"고 지적 — 실측 겹침(알약 하단 272 > 카드 상단 228, 2줄 문장일 때).
- **최종: 화살표를 헤드라인 텍스트 양옆에**(`.cr-lead-wrap` flex: `‹`+lead+`›`, `align-items:center`로 문장 수직중심에 정렬). **도트 제거**(3문장이라 위치표시 불필요). 화살표는 숨김 상태에서도 flex 자리를 차지 → 등장 시 레이아웃 이동 0. `.is-ready`로 노출. 침범 해소 실측: 화살표 하단 146 < 카드 228(데스크톱), 140 < 313(375px). 모바일은 화살표가 폭을 먹어 2줄 문장이 3줄로 늘지만 카드와 거리 충분·가로 스크롤 0. reduced-motion: 자동재생 없이 화살표만. no-JS: 정적 1번 문장(화살표 opacity:0).

**스킬 로고(요청 2) — 레벨바 유지로 결론.** 사용자가 "노션 로고가 링크로 첨부돼 있을 텐데 못 가져오나? 불가능하면 레벨바 유지"라 함 → **추출 시도**: ① 인앱 브라우저로 노션 접속 = 무거운 SPA라 navigate 300s 타임아웃(5분 행), ② claude-in-chrome(사용자 실제 로그인 브라우저)로 우회 시도 = `list_connected_browsers` 빈 목록(확장 미연결), ③ WebFetch = 노션 정적 HTML에 이미지 URL 없음(§2-9). **세 경로 모두 불가** → 사용자 폴백 지시대로 현행 레벨바 유지. 재개 조건: 사용자가 로고 파일 또는 각 이미지의 실제 URL을 제공. 그때 `.skill`에 로고 슬롯(+없을 때 graceful) + 테마 틴트로 드롭인. **교훈: 노션 자산 추출은 인앱 브라우저로 불가(SPA 행), Chrome 확장 연결이 전제.**

### 2-18. 이력서 우선순위 재배치 + 유튜브 다영상·조회수 (2026-07-20, 사용자 지시)

**이력서 벤토 재배치(우선순위 역전).** 사용자가 "GAME ENGINE이 가장 중요, EDUCATION이 가장 덜"이라 순서 재점검 요청. 파이썬으로 카드 블록 이동(어서션: rz-h 읽기순서 검증) — **Profile → Game Engine → Activity-Game → Awards → IT Service → Education**. 그리드: `.rz-skills` span 1/-1→**span 5**(Profile 옆 승격, `display:flex`+`.skills--engine{flex:1;align-content:center}`로 3배지 세로 정렬·카드 높이 채움), `.rz-edu` span 5→**1/-1**(최하단 풀폭), rz-side 내부 Awards↔IT 교체. 920px↓ 전부 풀폭(rz-skills 추가). 실측 1280px: Profile 619|Engine 437(높이 310 매칭) / Game 619|Side 437 / Edu 1072 풀폭.
- **미완: Awards 날짜(요청 1b).** "AWARDS·SCHOLARSHIP에 날짜 포함" 지시했으나 4개 수상(펄어비스·벨킨·카카오·WISH 해커톤) 날짜를 노션에서 확보 못 함 → **사용자에게 요청**. 장학은 이미 날짜 있음.

**유튜브 다영상 + 실시간 조회수.** h2 "취미는 게임 분석 유튜브"→"취미로 운영하는 게임 유튜브 채널", lede 교체(플레이/제작설명/언리얼 자습 등). 단일 임베드(`.ytb2`)를 **데이터 기반 카드 그리드**로 전환: `YT_VIDEOS` 배열([{id,title}]) → JS가 `.ytb-list`에 iframe 카드 렌더(`auto-fit minmax(300px)`, 영상 추가=배열만). 새 영상 `wBBHjFwxMkk`(여우숲 「공포는 서사가 된다」) 추가.
- **실시간 조회수(요청 5)**: `YT_API_KEY` 상수에 YouTube Data API v3 키를 넣으면 `videos.list?part=statistics`로 조회수 fetch→표시. **키 없으면 조회수 줄 숨김**(`.ytb-views:empty{display:none}`, 영상은 정상). oEmbed엔 조회수 없어 키 필수 — 키 없이 실시간은 불가(정직). 사용자에게 키 발급 or 수동 표기 선택 요청.

### 2-19. 리뷰 썸네일 파이프라인 + 목차 개편 + 유튜브 수치·GitHub 카드 (2026-07-20, 사용자 지시)

**★ 리뷰 썸네일 파이프라인(자가학습 지시 — 리뷰 추가 때마다 반복할 것).**
① 스토브 글을 인앱 브라우저로 열고 JS로 본문 첫 이미지 추출: `[...document.querySelectorAll('[class*=content] img, [class*=editor] img')]`에서 프로필·이모티콘·`partners-sns` 제외한 **첫 번째**가 대표 이미지(스토브 CDN `d2x8kymwjom7h7.cloudfront.net`).
② `curl`로 다운로드 → `sips --resampleWidth 800 -s format jpeg -s formatOptions 85` → `assets/img/reviews/<슬러그>.jpg`(현재 exp33.jpg·detroit.jpg, 각 1000×750 원본→800px 180~224K).
③ `.rvw` 카드 구조: `<img class="rvw-thumb">` + `<span class="rvw-body">`(기존 내용 랩). **함정: img의 width/height 속성 힌트가 `aspect-ratio:16/9`를 죽인다 → CSS에 `height:auto` 명시 필수**(이번에 526×600으로 깨진 걸 계측으로 잡음).

**목차 개편(요청 1).** `프로젝트 기획서/역기획서/제안서` 3링크 삭제 → `기획서 모음`(#projects)·`긴장 곡선`(#tension) 신설. 최종 6개: 이력서·기획서 모음·긴장 곡선·언리얼 레벨·분석·리뷰·추가 링크.

**유튜브 수치 = 방식 2(수동).** API fetch 코드 제거, `YT_VIDEOS`에 `views` 필드('3.6천회'/'1천회')·`YT_SUBS`('구독자 170명' — 유튜브 채널 페이지 curl 스크레이프로 현재값 확인) 데이터 상수화. **실시간(요청 3)은 YouTube Data API 키 없이는 불가**(oEmbed·스크레이프는 사이트 클라이언트에서 CORS 불가) — 수동 채택했으므로 갱신은 상수 2개만.

**GitHub 히스토리 공개(요청 4) = 가능, 링크 카드로 구현.** 리포(`00ny/00ny.github.io`)가 퍼블릭이므로 #links에 3번째 카드 `GitHub · 사이트 빌드 히스토리`(→ /commits/main) + lede에 "이 웹사이트를 만들어 온 과정은 GitHub 커밋 기록에" 문장 추가. `.links`는 `auto-fit minmax(280px,1fr)`로 3→2→1열. (심화안: GitHub API로 커밋 로그를 사이트에 직접 렌더 — 퍼블릭 리포는 CORS 허용, 60req/hr — 원하면 다음에.)

**긴장 곡선 브릿지(요청 5).** `직접 세운 기록 ↓`(#unreal) → `학습 과정: 블로그 글 ↗`(blog.naver.com/cl0udwoon/224333009653, 새 탭). "재는 법을 알면…" 문장은 유지.

**검증(2026-07-20):** 목차 6개 신구성·썸네일 2장 로드(16:9 526×296 — height:auto 수정 후)·조회수 2건·구독자 170명 표기·GitHub 카드 href(/commits/main)·블로그 링크·#unreal 구링크 소멸. 375px 전부 1열·가로 스크롤 0·콘솔 0. 비상단 스크린샷은 세션 내내 불가(기존 결함) — JS 계측으로 대체.

**프로필 사진 추가 (2026-07-20).** 이력서 Profile 카드에 본인 헤드샷(`assets/img/profile/bae.jpg`, ID사진 864×1184→480×657 JPEG q88 51K). `.rz-top` flex로 사진(94px, 3:4)+이름·직군 가로 배치, 스탯 타일은 아래. 프로필 카드 310→370px, 엔진 카드(span 5)가 align-content:center로 높이 매칭. **또 `height` 속성이 aspect-ratio 죽이는 함정 재발 → `.rz-photo{height:auto}`로 수정**(§2-19 리뷰썸네일과 동일 교훈 — img에 width/height 속성 주면 CSS에 height:auto 필수. 세 번째라 규칙으로 굳힘).

### 2-21. 캐러셀 게임명 칩 + 자동 넘김 + 히어로 문장 한 줄 (2026-07-20, 사용자 지시)

**① 원작 게임명 칩.** 역기획·제안 카드가 "무슨 게임인지" 안 읽힘 → `data-game` 속성 신설(가마솥=호라이즌: 제로던, 무릉·헤스티=명일방주: 엔드필드). 캐러셀 `render()`가 `.cr-chip--game`에 표시(없으면 `display:none`). 3단 칩 위계: kind(솔리드 흰색) → **game(옅은 흰 채움 .15)** → field(외곽선). 창작 2장(원쁠원·여우숲)은 원작 없어 미표시. 헤스티는 제안서지만 엔드필드 기반이라 포함(요청은 역기획서였으나 동일 문제라 확장).

**② 자동 넘김.** 히어로 문장 회전(§2-16) 완료 시 `revealArrows()`가 `document.dispatchEvent('hero-rotation-done')` → 캐러셀 IIFE가 수신해 `heroReady=true`. 이후 **2초마다 `go(1)`**(=콘텐츠 왼쪽으로, act 증가 = 중앙카드가 좌로 밀림). 조건: `heroReady && heroVis(IntersectionObserver 0.4) && !hover(stage pointerenter/leave)`. `go`가 매번 `armAuto()` 재예약 → 수동 조작 시 타이머 리셋(직후 튐 방지). reduced-motion=회전 없이 바로 준비. 검증: 여우숲→가마솥→무릉→헤스티→원쁠원 순환·호버 5초 정지 유지.

**③ 히어로 문장 2 한 줄.** "기획의도가 가장 중요하다고 배웠습니다." 한 줄 폭 746px인데 화살표 사이 여유 744px = **딱 2px 초과**로 접힘 → `.cr-kick` width min(92vw,**920px**)로 넓혀 해결(여유 784px). **전역 줄바꿈 검토**: 렌더되는 실제 `<br>`은 2곳뿐 — `.umap-wip`(블록아웃 제작 중<br>… → ` · `로 한 줄) 제거, **히어로 문장 3의 `<br>`은 유지**(긴 펀치라인의 의도적·가독성 있는 분리 — "가독성 우선" 원칙에 부합). 나머지 줄바꿈은 소스 개행(=공백)이라 렌더 영향 없음.

**검증(2026-07-20):** 게임 칩 3장 표시/2장 숨김·자동 넘김 순환·호버 정지·문장2 1줄(920px)·wip 1줄·데스크톱/375px 가로 스크롤 0·콘솔 0.

### 2-22. 자동 넘김 감속 + 플레이 카드 통일·전투력 하이라이트 + 그리고 리스트 (2026-07-20, 사용자 지시)

**① 자동 넘김 감속.** 너무 빠르다 → 간격 2000→**4500ms**, 카드 트랜지션 `.55s cubic-bezier(.22,.61,.36,1)`→**`.85s cubic-bezier(.4,0,.2,1)`**(ease-in-out, 부드러운 글라이드).

**② 플레이 카드 통일 + 전투력 하이라이트.** 문제: 엔드필드의 `만렙·고인물` 칩이 **이름과 LV 사이**에 들어가 그 카드만 LV가 밀려 정렬이 깨졌다. 해결: `PLAY_GAMES` 데이터를 `note`(상태칩)·`power`(전투력)·`detail`(부가)로 분리, 렌더를 **name → `.plv-row`(LV + 상태칩 같은 줄, flex-wrap) → sub** 3단으로 통일. 상태칩이 LV 줄로 가서 **모든 카드 LV/이름 top 픽셀 일치**(계측 확인). 전투력은 `.ppw--hot`(accent-strong 앰버·볼드)로 하이라이트해 "중요함 어필".

**③ "그리고" 한 줄씩 + 2게임 추가.** 인라인 `·` 나열(`.play-etc`)이 대충 → **`PLAY_MORE` 배열 + `.play-more-list`(한 줄에 하나)**. 메이플스토리(최고레벨 캐릭터 렌 LV.256)·아이온2(LV.43) 추가, 총 8줄. 디트로이트·33원정대는 `review:true`로 `리뷰 ↑` 링크.

**검증(2026-07-20):** 카드 5장 LV top 전부 10946px 일치·전투력 앰버(rgb 245,164,92)·그리고 8줄·트랜지션 0.85s·375px 2열·가로 스크롤 0·콘솔 0. 데스크톱 비상단 캡처 불가라 모바일 스크린샷으로 육안 확인.

## 3. 검증 결과 (2026-07-15)

- base64 인라인 미디어 **224개 전량 추출** → HTML 30.8MB → **273KB** (이미지 22MB + 영상 744KB 분리)
- 로컬 참조 **232개 고유 URL 전부 HTTP 200**, 깨진 참조 0
- 6개 페이지 이미지 **272개 전부 로드 성공**, 실패 0, 콘솔 에러 0
- 추출한 mp4 재생 가능(readyState 4)
- **375px 모바일 6개 페이지 전부 가로 스크롤 0**
- 이전/다음 체인 5개 + 왕복 정합 통과

### 캐러셀 개편 검증 (2026-07-16)

- 순환 5장 무한 루프(5번 넘기면 제자리) · 타이틀·칩·태그라인·배경 에코 동시 전환 통과
- 측면 카드 클릭 = 중앙 이동만(preventDefault) / 중앙 카드 클릭 = 실제 페이지 이동 확인
- hover: 측면 카드 펴짐 + 메타 페이드인 **육안 확인** (요구사항 1 충족)
- 키보드 ←/→ 순환, Tab은 중앙 카드에만 도달(측면 tabIndex=-1)
- 드래그: 좌→다음 / 우→이전 / 40px 미만 무시 (실제 PointerEvent로 검증)
- 목차 앵커 5개 전부 목차 바(52px) 아래 16px 여백에 착지
- 375px: 평면 모드(3D 회전 없음) · ±2 숨김 · 메타 상시(중앙 제외) · 가로 스크롤 0
- no-JS: 카드 5장 세로 배치, 링크 5개 생존, 가로 스크롤 0
- 참조 234개 전부 HTTP 200 · 콘솔 에러 0

**2차 보완 검증 (2026-07-16):** 순환 5장·성격/분야 칩 동기·제목 동기 통과. 순서 3곳(캐러셀·카드·셸 체인) 일치 확인.
셸 중복 주입 0, 왕복 정합 통과. 참조 234개 HTTP 200, 콘솔 에러 0, 375px 가로 스크롤 0(칩 2개 한 줄 유지).

**헤스티 교체 검증 (2026-07-17):** 새 페이지 이미지 45개 전량 로드·실패 0, Pretendard static + Hahmlet 폰트 적용,
site-bar(05/05)·proj-nav(이전=무릉, dir색 오렌지 상속) 정상, OBSERVED 3색 라벨 고유값 유지.
순서 3곳 일치, 셸 중복 0, 무릉 next=새 파일명. 참조 235개 HTTP 200, **고아 이미지 0**(옛 헤스티 자산 완전 교체), 콘솔 0, 375px 가로 스크롤 0.

**다크 리뉴얼 검증 (2026-07-17):** index body #0E0F13·카드 #16181E·푸터 #0B0C0F, 히어로 100svh(데스크톱·모바일), 섹션 투명→흰 띠 0,
넘버링 01–05 연속, 캐러셀 순환 작동(JS 무손상), 이력 링크 엠버. 390px 가로 스크롤 0, 모바일 인디케이터 숨김.

**전체 다크 검증 (2026-07-17):** 5개 프로젝트 페이지 전부 body #0E0F13, site-bar #20252E(slate)·proj-nav #0E0F13(paper) 다크 상속,
각 페이지 accent 밝은 버전으로 대비 확보, 헤스티 3색 라벨(obs/inf/des) 밝게·sage 밴드 어둡게, 원본 기획서 이미지는 발광체로 표시.
**index↔프로젝트 무단절**(둘 다 다크 — §10 흰 띠 0 전 사이트 충족). 참조 235개 HTTP 200, 콘솔 0. 캐러셀/라이트박스 JS 무손상.

**이력 리뉴얼 검증 (2026-07-18):** 날짜칩 폭 118px 정렬축, 스킬 레벨바 22종 on 개수=Lv 전수 대조 일치, 하이라이트 3건만 엠버,
수상 2열 그리드(등급만 강조), 게임엔진 배지 링크(foxforest·onepluone) 정확. 390px 스킬 1열·가로 스크롤 0, 콘솔 0, 참조 실패 0.

**7건 개편 검증 (2026-07-19):** 벤토 데스크톱(1280px) 배치 실측 — Profile 619px | 학력 437px(높이 349 동일), 게임 880px | 우측 스택 880px(높이 일치), 스킬 풀폭 1072px. 분석 그리드 616px | 440px. 390px 모바일: 전 패널 1열(342px)·스탯 2×2·가로 스크롤 0.
칩·목차·그룹명·카드 chip 전부 새 명칭 동기, 연락처 링크 1개(이메일), hot 3건 유지, 스킬 24개(22바+엔진 2), 수상 4건 출처 표기.
유튜브 iframe 실제 썸네일 렌더 확인, 리뷰 카드 제목 흰색(수정 후), 콘솔 에러 0, 깨진 이미지 0.

**워딩·마커·스킬 조정 검증 (2026-07-19):** 수상 .for 4건 육안 확인(기업 솔루션 접두 2·WISH 해커톤·여행 앨범 앱), 마커 3곳(스마일게이트·게임 리뷰 보러가기 ↓·NC AI) 앰버 알약 렌더, Lv.10 6종 노랑 바+배지(#F5C84B)·나머지 흰색 대조, 스킬 2열(데스크톱 502px×2)·모바일 1열, 엔진 배지 앰버와 Lv.10 노랑 구분. 1280px·390px 가로 스크롤 0, 콘솔 0, 모바일 마커 알약 무손상 줄바꿈.

**도구 한계 메모(다음 세션이 헛수고 안 하도록):** 브라우저 페인의 `left_click`은 해시 앵커 이동을 일으키지 않고,
`left_click_drag`는 `pointerup`을 전달하지 않는다. 둘 다 페이지 결함이 아니며, JS 이벤트 디스패치로 검증했다.
또 가상 마우스가 카드 위에 얹혀 있으면 `:hover`가 유지돼 오판하기 쉽다 — 확인 전에 마우스를 빈 곳으로 옮길 것.
`resize_window` 후 스크린샷이 축소돼 깨지면 프리뷰를 재시작하고 폭을 다시 지정하면 복구된다.
**(2026-07-19 추가)** 스크린샷이 새까맣게 나오는 건 페이지 결함이 아니라 캡처 파이프라인 결함이다 — 레이아웃은 JS 계측(getBoundingClientRect)으로 먼저 확인할 것.
비네이티브 해상도(예: 1280×800 지정)에서 캡처가 자주 죽고, `location.reload()` 후 뷰포트가 0×0으로 붕괴하기도 한다(이러면 bodyH가 수만 px로 튀어 계측도 오염됨).
휠 `scroll` 액션은 30초 타임아웃 후 캡처를 재차 죽인다. 복구 순서: `preview_stop`→`preview_start`→(필요시) `resize_window preset:desktop`. JS `scrollTo`+스크린샷 조합은 세션에 따라 되기도 안 되기도 한다.

## 4. 원칙 (유지)

- 일관성 > 화려함. 공통 요소는 `assets/style.css` 단일 소스로만 통제.
- 정보 위계 = 스캔 → 깊이 (Input→Condition→Output 철학의 시연).
- 실제 콘텐츠는 추측하지 않고 사용자에게 요청.
- 프로젝트 페이지 본문은 **의미 변경 금지**. 구조적 주입(셸)과 기계적 치환(경로·토큰명)만 허용.
