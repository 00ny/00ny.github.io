# 첫 화면 개편 v2 — 5건 지시 설계서

> 작성 2026-07-28 (맥). 기준 커밋 `c1f57e5`. 사용자 지시 5건 + 확정 응답 4건을 반영한 **기획·설계 문서**다.
> 구현은 별도 세션에서 진행한다(라우팅: 설계·감리·핵심 편집 = Opus 5 / 이미지 가공·조회·계측·빌드·시드 재추출 = runner(Haiku)).
> 선행 문서: [FRONT_PAGE_PLAN.md](FRONT_PAGE_PLAN.md)(콕핏 v1), [HANDOFF_MAC.md](HANDOFF_MAC.md), CLAUDE.md §2-27.

---

## 0. 지시 5건과 확정 응답

| # | 지시 | 확정 응답 |
|---|---|---|
| 1 | 레벨 역기획+긴장 곡선 묶고 [언리얼] 맵 제작만 분리. 첫화면 버튼은 위아래 유지 | 페이지 순서 전면 재배치로 확장(§1 IA v4) |
| 2 | 요약 이력서 ↔ 전체 이력서 **동시 존재 금지** + 스크롤 확대/페이드 연출 + 이메일 클릭 복사 + 사진 교체 + 프로젝트·서비스 강조 | 사진은 **원본 그대로**(아이들 포함 — 사용자 결정, 초상권 고지 완료) |
| 3 | 첫화면 정렬·통일성 재설계 | 배지 3층 → **행엔 상태점만**, 성격·원작·엔진은 프리뷰로 |
| 4 | Projects hover 썸네일 (캐릭터 선택창 느낌) | **영역 전체 배경 에코** 방식 |
| 5 | 학력 밑에 `2023.10 병834기 공군 전역` 추가 | — |

**★ 해석 1건 — 확인 필요.** 응답의 "분석-제안서"를 **제안서 섹션(헤스티·니달리)**으로 해석했다.
스토브 리뷰(#analysis)·유튜브(#youtube)는 기존 위치(프로젝트 섹션들 뒤) 유지로 간주한다. 오독이면 §1 순서만 바꾸면 된다.

---

## 1. IA v4 — 페이지 순서 재배치

> 응답 원문: "역기획서 '퀘스트-레벨디자인' 순서 시작으로, 긴장 곡선(바로 위의 레벨 디자인과 묶어서), 분석-제안서, 제작 프로젝트[유니티 3D] [MSW 2D], 그리고 마지막에 [언리얼 3D]"

```
목차바 → #top 콕핏 히어로 → 사이 띠 → #resume
→ #reverse   역기획서        가마솥(퀘스트) → 무릉(레벨) → 긴장 곡선(#tension, 무릉과 한 묶음)
→ #proposal  제안서           헤스티 → 니달리
→ #projects  제작 프로젝트    여우숲(유니티 3D) → 원쁠원(MSW 2D)   ← 순서 주의: 유니티가 먼저
→ #unreal    [언리얼] 맵 제작  MAP 03 → 02 → 01 (§2-27 순서 유지)
→ #analysis → #youtube → #skills → #links → #play → #showcase → 푸터
```

- **#leveldesign 섹션 폐지.** 무릉은 #reverse로, 언리얼 3종은 독립 섹션 #unreal로. `#leveldesign` 참조 전수 치환(히어로 언리얼 버튼 → `#unreal`, 목차). `#tension`·`#unreal` id는 앵커 호환 유지.
- **#proposal 섹션 신설** (기존 #reverse에서 헤스티·니달리 분리).
- **제작 프로젝트 내부 순서 반전**: 현행 원쁠원→여우숲을 **여우숲→원쁠원**으로 (응답의 [유니티][MSW] 순).
- **첫화면 버튼**: 배치 불변 — 레벨 디자인 그룹에 언리얼·무릉 버튼이 위아래로 붙어 있음(지시 1 충족). 언리얼 버튼 href만 `#unreal`로.
- **목차 7개로 재설계**(6→7, 섹션이 늘어 불가피): `이력서 · 역기획서 · 제안서 · 제작 프로젝트 · 언리얼 레벨 · 분석 · 리뷰 · 추가 링크`. 375px 랩 검증 필수.
- **넘버링**: 섹션별 01 재시작(역기획 01-02 / 제안 01-02 / 제작 01-02). 긴장 곡선 카드(.tsn)는 카운트 밖(§2-15 유지).
- 각 섹션 헤더: #reverse `Reverse-engineered / 역기획서`, #proposal `Proposal / 제안서`, #projects `Built / 제작 프로젝트`, #unreal `Unreal / [언리얼] 맵 제작`(사용자 명명 그대로). lede는 구현 시 초안 작성 → 사용자 검수 목록에 올림.

### 관리자 마커 재편 (v3)

| 마커 | 변경 |
|---|---|
| `PROJECTS_REVERSE` | 내용 = 가마솥 → 무릉 (무릉이 LEVEL에서 이사) |
| `PROJECTS_PROPOSAL` | **신설** = 헤스티 → 니달리 |
| `PROJECTS_BUILT` | 순서 반전 = 여우숲 → 원쁠원 |
| `PROJECTS_LEVEL` | **폐지** |
| `UNREAL`·`FOOT_COUNTS`·기타 | 위치만 이동, 내용 무영향 |

admin `area` enum: `built / reverse / proposal` (level 제거). 무릉 `level→reverse`, 헤스티·니달리 `reverse→proposal` 마이그레이션. 시드 재추출 필수(§6).

---

## 2. 첫화면 정렬 시스템 (지시 3)

문제 진단: ① 좌 요약 카드와 우 버튼 영역의 상·하단이 안 맞음 ② 버튼마다 배지 개수가 달라 제목 시작선이 들쭉날쭉 ③ 그룹 헤더·간격 불균일.

```
.hx  grid 5fr 7fr, gap 28, align-items:stretch, min-height:100svh
좌 .hz-card   flex column — 꼬리 줄(.hz-more)에 margin-top:auto → 우 패널과 바닥 일치
우 .hz-proj   (신규 래퍼) position:relative, overflow:hidden, 카드와 동일 radius/border
  ├ 헤더 행(40px 고정): "Projects" 라벨(11.5px mono) ←→ .pseg 토글. 하단 1px line
  ├ .hz-bg ×2 (absolute 크로스페이드 — §3 배경 에코) + 스크림
  ├ .pbtn-set — 그룹 헤더 + 버튼 행 (아래 해부)
  └ .hz-cap  하단 캡션 스트립(34px 고정 — §3 메타 표시 자리. 비어도 높이 유지 = 레이아웃 이동 0)
```

**버튼 행 해부 — 배지 3층 폐지**

```
.pbtn  grid-template-columns: 1fr auto; 높이 균일 54px; 패딩 좌 14
  1열: .pbtn-t 15.5px/600 (모든 행 같은 x에서 시작)
       .pbtn-sub 12.5px --text-sub
  2열: 상태점만 — 원쁠원·여우숲 한정 (● #4ADE80 서비스 중 / ● 회색 종료 + 9px 라벨)
hover/focus: 좌측 2px 앰버 바 + 행 배경 rgba(255,255,255,.04) + 배경 에코 발동
```

- 성격(제작/역기획서/제안서)·원작·엔진 배지는 행에서 **제거** → 캡션 스트립(§3)으로 이동.
- **모바일(<760px)·no-JS에서는 배지 복원**(에코·캡션이 없으므로 정보 손실 방지). 마크업엔 배지를 그대로 두고 데스크톱 CSS로 숨기는 방식 — 관리자 렌더러가 조건 분기를 안 해도 된다.
- 그룹 헤더 통일: 11px mono 대문자 --text-sub, 상 16 하 6, 첫 그룹만 상 0.
- 간격 토큰: 4/8/12/16/24만 사용. 임의값 금지.
- 1280×800 수납 재검증 필수(§2-27 당시 775px — 행 해부가 바뀌므로 재실측). 초과 시 행 높이 54→50 우선 조정.

---

## 3. 배경 에코 프리뷰 (지시 4)

캐러셀 배경 에코(§2-6 `.cr-bgwrap` 2장 크로스페이드) 패턴을 우측 패널에 이식한다.

- `.hz-bg` 2장 absolute + `object-fit:cover`. hover/focusin 시 대상 프로젝트 썸네일로 **280ms 크로스페이드**, opacity 최종 `.22`.
- 스크림 `linear-gradient(rgba(14,15,19,.78), rgba(14,15,19,.92))` 상시 — 텍스트 대비 AA 보장(에코가 어떤 이미지든).
- `.hz-cap` 캡션 동기 갱신: `[성격 pill] 원작/엔진 · 태그라인` (예: `역기획서 · 명일방주: 엔드필드 · 물 하나로 공간을 잠그고 연다`).
- pointerleave: **마지막 항목 유지**(캐릭터 선택창 관례). 초기 상태: 배경 없음 + 캡션 `프로젝트에 올려 미리보기` (옅게).
- 데이터: `.pbtn`에 `data-thumb / data-kind / data-sub / data-tagline` 속성(관리자 렌더러가 출력 — §6). 썸네일은 기존 자산 재사용:

| 버튼 | data-thumb |
|---|---|
| 언리얼 | `assets/img/unreal/` 내 블록아웃(수영장 우선 — 구현 시 runner가 경로 확정) |
| 무릉 | `assets/img/endfield-level/01.webp` |
| 여우숲 | `assets/img/foxforest/carousel.jpg` |
| 원쁠원 | `assets/img/onepluone/01.jpg` |
| 가마솥 | `assets/img/hzd-quest/01.webp` |
| 헤스티 | `assets/img/hesti-character/carousel.jpg` |
| 니달리 | `assets/img/nidalee-character/carousel.jpg` |

- 로드 후 `Image()` 7장 프리로드(첫 hover 깜빡임 방지). 터치·모바일: 에코 생략(탭=즉시 이동). reduced-motion: 페이드 대신 즉시 교체.

---

## 4. 이력서 배타 연출 (지시 2)

**원칙: 요약 이력서와 전체 이력서는 어떤 스크롤 위치에서도 동시에 보이지 않는다.**

### 4-1. 스크롤 안무 (H = 히어로 실측 높이)

| 대상 | 가시 구간 | 연출 |
|---|---|---|
| 요약 카드 `.hz-card` | scrollY ∈ [0, 0.28H) | opacity 1→0 + scale 1→1.04 (transform-origin 좌상단). **양방향** — 올라오면 복원 |
| 전체 이력서 `#resume .wrap` | scrollY ∈ (0.45H, ∞) | 기본 opacity 0. 최초 도달 시 `.is-open` 1회(카드 stagger 40ms/620ms — §3-1 v1 재사용). 이후 opacity는 스크롤 연동 clamp((y−0.45H)/0.15H) **양방향** |

- **서로소 증명**: 요약 가시 [0, 0.28H) ∩ 이력서 가시 (0.45H, ∞) = ∅. 어느 방향 스크롤이든 성립.
- 이력서의 h2·tag까지 `.wrap` 게이트 안에 넣는다(초반 슬쩍 보임 방지).
- 단일 scroll 리스너 + rAF 스로틀, transform/opacity만. 로드 시 현재 scrollY로 상태 초기화(중간 새로고침·앵커 점프 대응). 목차 "이력서" 클릭 → 착지점이 0.45H 이상이라 정상 발동.
- reduced-motion: stagger·scale 생략, 문턱 즉시 전환. **no-JS: 둘 다 정적 표시(배타 규칙의 유일한 예외 — 명기)**.
- 버튼 패널·히어로 문장은 자연 스크롤(연출 없음). `hero-rotation-done` 발신 불변(§2-27 ④).

### 4-2. 전체 이력서 개편

- **강조**: `.rz-stats`의 `게임 프로젝트 5`·`게임 서비스 2` 타일을 히어로 대타일과 동형으로(2칸 폭 + 큰 숫자 + `--accent-strong`). 역기획·제안 4 / 수상 4는 소타일 유지. 동형 반복이 "커졌다" 착시(§3-1 v1)도 강화한다.
- **이메일 클릭 = 복사**: `<a href="mailto:...">` 유지 + JS 인터셉트. clipboard 성공 → preventDefault + 토스트 **"이메일이 복사되었습니다."**(하단 중앙 pill, 1.5s 표시 + 0.25s 페이드, `aria-live=polite`). 실패(비보안 컨텍스트 등) → 기본 mailto 동작. no-JS → mailto.
- **사진 교체**: `assets/img/profile/bae-kids.jpg` (워크숍 원본 — **원본 그대로**, 사용자 결정). Profile 카드 재구성: 상단 와이드 미디어(16:10, 본인 좌 + 아이들 우 전폭 크롭) → 이름 행 → 스탯 → 이메일. 가공: sips 1200w q85. **`img` width/height 속성 시 CSS `height:auto` 필수**(세 번 밟은 함정). 히어로 요약의 94px 증명사진(bae.jpg)은 유지 — 상단 공식/하단 친화의 대비가 의도.
- **⚠ 원본 파일 미수급.** 채팅 첨부는 파일로 저장되지 않는다 — 사용자가 원본을 `~/Downloads` 또는 저장소 안에 저장해 줘야 착수 가능(§8).

---

## 5. 군 복무 줄 (지시 5)

- 히어로 요약 `hz-edu` 아래 둘째 줄: 칩 `2023.10` + `병834기 공군 전역`.
- 전체 이력서 Education 카드에도 동일 항목(`23.10` 칩 — 그 카드의 날짜 표기 관례에 맞춤). `RESUME_EDU` 마커 안이므로 파일+시드 동시 반영.
- admin: `HERO_STATS` 렌더러에 settings `hero_edu2_date`/`hero_edu2_text` 추가(비면 줄 생략).

---

## 6. 관리자 동기화 (배포 전 필수)

1. 마커 재편(§1 표): `renderProjectArea`에 `proposal` 추가·`level` 제거, publish 배선 갱신.
2. `renderHeroButtons` v2: 새 행 해부(제목+부제+상태점) + `data-thumb/kind/sub/tagline` 속성 출력. 배지 마크업은 유지 출력(모바일 복원용 — CSS가 숨김). `hero_button` 필드에 `thumb`·`tagline` 추가.
3. `HERO_STATS`: `hero_edu2_*` settings.
4. `project.entity` area 마이그레이션(무릉·헤스티·니달리).
5. **시드 재추출**(`scratchpad/extract_seed.py` — 새 구조 반영해 갱신 필요. §2-27 ⑨ 절차).
6. **정합성 검증**: 사본 대상 실제 publish → 바이트 비교(§2-27 ⑦ 방법 그대로).

에코 배경·캡션 스트립·연출 JS·토스트는 전부 **마커 밖**(셸) — 관리자 무관, 대화로 관리.

---

## 7. 구현 순서 · 라우팅

> 사용자 방침: 제작은 **Opus 5 + Haiku(runner)로만**. Opus 5는 턴당 코드 2파일 게이트 안에서 직접 편집(index.html+style.css가 정확히 2개).

| 단계 | 내용 | 담당 |
|---|---|---|
| P0 | 사진 원본 수급(사용자) → 크롭·리사이즈 가공, 에코 썸네일 경로 확정 | runner |
| P1 | IA v4 블록 이동 — Opus 5가 파이썬 수술 스크립트 작성(어서션: 카드 수·마커 쌍), runner 실행·검증 | Opus 5 + runner |
| P2 | 첫화면 정렬 + 배경 에코 (index.html·style.css) | Opus 5 |
| P3 | 이력서 배타 연출 + 이메일 복사 + 사진 + 스탯 강조 + 군 복무 (index.html·style.css) | Opus 5 |
| P4 | 관리자 동기화 §6 (renderers+sections / entity+seed 등 턴 분할) | Opus 5 + runner(빌드·재추출) |
| P5 | 검증 sweep + 단계별 커밋 | runner + Opus 5 |

### 검증 항목

- 섹션 순서 = §1, 목차 7개 앵커 착지, `#leveldesign` 잔존 참조 0
- 버튼: 제목 시작 x 동일 · 행 높이 54 균일 · 좌우 패널 상하단 일치(px) · 1280×800 수납 · 토글 양세트 동일 규격
- 에코: hover 크로스페이드·캡션 동기·초기 상태·프리로드·터치 생략·대비 AA
- 배타: scrollY 스윕하며 요약·이력서 동시 가시 프레임 0 (JS 계측) · 역스크롤 복원 · 앵커 점프 · reduced-motion
- 이메일 복사 실동작 + 토스트 · mailto 폴백
- 사진 로드·비율(height:auto) · 군 복무 2곳 · 스탯 동형 강조
- 관리자: 사본 publish 바이트 비교 · 니달리 잔존 · 캐러셀/자동 넘김 무손상(`hero-rotation-done`)
- 1280·1000·375px 가로 스크롤 0 · 콘솔 0 · 참조 이미지 전량 로드

---

## 8. 대기 · 리스크

| 항목 | 상태 |
|---|---|
| **사진 원본 파일** | ★ 사용자 제공 대기 — 받기 전 P0·P3 사진 부분 착수 불가 |
| **"분석-제안서" 해석** | ★ 제안서 섹션으로 해석(§0) — 확인 요청 중 |
| 목차 7개 확장 | 내 판단(6개 유지 불가능해짐) — 되돌리기 쉬움 |
| 섹션 lede 문안 | 구현 시 초안 → 사용자 검수 목록 |
| 미성년자 초상권 | 고지 완료, 사용자가 원본 사용 결정. 문제 제기 시 블러본 교체(가공 스크립트 보관) |
| 에코 성능 | 이미지 7장 프리로드 ~1.5MB — lazy 아닌 load 후 지연 프리로드로 첫 페인트 무영향 |
| 배타 연출 ↔ 기존 is-open 충돌 | v1 IO 로직을 §4-1로 대체(공존 금지 — 이중 트리거 방지) |
