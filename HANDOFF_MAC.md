# 맥으로 이어받기 — 인수인계

> 작성 2026-07-28 (Windows PC에서 중단). 기준 커밋 **`bf537a5`("중단")**.
> 이 문서 하나만 읽으면 이어서 작업할 수 있게 썼다. 설계 근거는 [FRONT_PAGE_PLAN.md](FRONT_PAGE_PLAN.md), 사이트 구조는 [README.md](README.md), 이력은 [CLAUDE.md](CLAUDE.md).

---

## 0. 맥에서 처음 할 일 — 모델 라우팅 설정

**이게 안 되어 있으면 토큰이 지난 세션처럼 녹는다.** Windows PC에서는 고쳤지만 맥의 `~/.claude/`는 별개 파일이라 안 넘어온다.

맥의 `~/.claude/opus5/opus5.md` (그리고 `~/.claude/CLAUDE.md`)의 마지막 줄
`서브에이전트에 위임할 때는 model을 명시하는 편이 안전합니다.` 를 아래로 교체할 것:

```
model은 반드시 위 라우팅 기준에 맞춰 명시하세요. 임의로 고르지 마세요.

- **잡일에 opus 를 절대 쓰지 마세요.** 문자열 치환·파일 이동·번호 갱신·빌드·조회·
  로그 확인·검증 grep·스크린샷 확인은 전부 runner (Haiku)입니다.
- 구현·수정·테스트·디버깅·리뷰는 model 을 지정하지 않습니다(실행 에이전트 기본값).
- opus 는 설계·아키텍처 결정·근본원인 분석에만 지정합니다.
- 메인 에이전트가 직접 돌리는 조회·검증도 마찬가지입니다 — 여러 번 반복되는
  grep·파일 확인·빌드는 본인이 하지 말고 runner 에 넘기세요.

애매하면 항상 낮은 쪽을 고르세요. 낮게 잡아 실패하면 올리면 되지만,
높게 잡은 토큰은 되돌릴 수 없습니다.
```

**두 파일 다 고칠 것.** `opus5 OFF/ON` 토글이 `CLAUDE.md`를 통째로 덮어쓰므로, 템플릿(`opus5/opus5.md`)을 안 고치면 다음 토글에 사라진다.

### 이번 세션에서 실제로 잘못 태운 예 (같은 실수 방지용)

| 작업 | 잘못 쓴 모델 | 맞는 모델 | 소비 |
|---|---|---|---|
| 프로젝트 페이지 5개 `05 → 06` 치환 + 카드 1장 추가 | opus | **runner** | 91k |
| 이력서 항목 추가 | opus | 실행 에이전트 기본 | 105k |
| 니달리 페이지 작성 | opus | 실행 에이전트 기본 | 200k |
| 첫 화면 개편 | opus | opus (이건 맞음) | 227k |

---

## 1. 지금 어디까지 되어 있나

### 완료 (커밋됨)

- **`993d652`** — 니달리 제안서 페이지 신규(6번째 프로젝트) · 슬라이드 16장 · 셸 체인 `05→06` · 이력서 확장(동아리 3 + 대외활동 4 패널, 수상 4건 날짜, 스탯 타일 교체)
- **`bf537a5`** — **첫 화면 전면 개편**. 콕핏 히어로(요약 이력서 + 직군별/엔진별 7버튼 토글), IA v3 13블록 재배치, 캐러셀 하단 이관, 이력서 확대 연출

현재 섹션 순서:
```
목차바 → #top(콕핏 히어로) → 사이 띠 → #resume → #leveldesign → #projects
      → #reverse → #analysis → #youtube → #skills → #links → #play → #showcase(캐러셀) → 푸터
```

- ADMIN 마커 **18쌍** (기존 13 + `RESUME_EXTRA` + `HERO_STATS` + `HERO_BUTTONS` + `PROJECTS` 1쌍이 `PROJECTS_BUILT`/`_REVERSE`/`_LEVEL` 3쌍으로 분할)
- 워킹 트리 깨끗함. **아직 push 안 됨** — 배포하려면 `git push origin main`

### 미착수

아래 §2·§3·§4. **§2가 사용자가 마지막으로 지시한 작업이다.**

---

## 2. 남은 작업 ① — 역할 우선 이름 체계 (최우선)

### 배경

인사팀장 피드백: **"각 프로젝트의 제목보다 역할을 강조하라. '무릉 천정원 지하'보다 '레벨 디자인 역기획서'가 더 중요한 정보다."**
지금은 큰 글씨가 프로젝트 고유명이고 역할이 작은 배지로 밀려 있다. 이걸 뒤집는다.

**한 글자도 시작되지 않았다.** 이전 시도는 구조 파악 단계에서 세션 한도로 끊겼고 파일 흔적 0건이다.

### 확정된 이름 표

**규칙: 역할이 제목. 역할명이 겹칠 때만 대상을 접두한다** (제안서 2건이 그 경우).

| 대상 | 제목 (큰 글씨) | 부제 (작은 글씨) |
|---|---|---|
| 언리얼 레벨 | `레벨 디자인 — 언리얼 제작` | `FPS 맵 3종 · 모작 → 창작 → 진행 중` |
| 무릉 | `레벨 디자인 역기획서` | `무릉 천정원 지하` |
| 여우숲 | `시스템 · 콘텐츠 통합 기획` | `여우숲` |
| 원쁠원 | `보스전 전투 · 데이터 기획` | `원쁠원` |
| 가마솥 | `퀘스트 디자인 역기획서` | `가마솥 SIGMA` |
| 헤스티 | `헤스티 캐릭터 제안서` | `신규 6성 뱅가드` |
| 니달리 | `니달리 응용 캐릭터 제안서` | `신규 캐릭터 · 비선형 퀘스트` |

`니달리 / Suzi` 표기는 **첫 화면에서 전부 없앤다**(사용자: "제목으로써도 탈락").

### 확정된 사용자 답변

| 항목 | 확정 |
|---|---|
| 표기 형태 | **두 줄** — 역할이 제목, 프로젝트명이 부제 |
| 제작 2편(원쁠원·여우숲) | 역기획서·제안서와 **같은 규칙** 적용 |
| 적용 범위 | **첫 화면 · 카드 · 캐러셀만.** `projects/*.html` 6개 내부(상단바·이전/다음·h1)는 **건드리지 않는다** |

### 적용 위치

고치는 파일은 **`index.html` 과 `assets/style.css` 둘뿐.**

**① 버튼 그리드** (`ADMIN:HERO_BUTTONS` 마커 안 `.pbtn` 7개 × 2세트 = 14개)
- `.pbtn-t` = 제목, 그 아래 `.pbtn-sub` 신규 요소 = 부제 (한 단계 작게, `--text-sub`)
- 기존 배지(성격 / 엔진·원작 / 서비스 상태)는 유지
- ⚠️ **버튼 높이가 늘어난다.** 개편 직후 1280×800에서 마지막 버튼 하단이 777px였다(뷰포트 800px). 부제가 붙으면 넘칠 수 있으니 계측하고, 넘치면 버튼 세로 패딩·그룹 간격으로 흡수할 것

**② 프로젝트 카드** (`.card` 7장)
- `.card__title` = 제목, 그 아래 `.card__sub` 신규 = 부제
- **`chip--field` 제거** — 새 제목과 중복된다(예: 여우숲의 `시스템 · 콘텐츠 통합` 칩). `chip--kind`는 유지
- `.card__concept`·`.card__more`는 그대로

**③ 캐러셀** (`ADMIN:CAROUSEL` 마커 안 `.cr-card` 6장 + 렌더 JS)
- `data-title` = 제목, `data-project` 속성 신설 = 부제
- `render()` 가 `.cr-big` 아래 `.cr-proj`(신규)에 `data-project`를 넣도록 수정. 값 없으면 `display:none` (기존 `data-game` 처리와 동일)
- `data-tagline`은 유지
- **`.cr-chip--field` 숨김** — 제목과 중복. `kind`·`game` 칩은 유지
- `.cr-card__meta`(측면 카드 hover 메타)의 `<b>`도 제목으로
- ⚠️ **3D 회전·자동 넘김·배경 에코·스와이프 로직은 건드리지 말 것.** 텍스트 슬롯만 늘린다 (CLAUDE.md §2-6·§2-7·§2-21)

**④ 목차 바** — 이미 역할 기반(`레벨 디자인`·`제작 프로젝트`·`역기획`)이라 **변경 없음**

### 검증

1. 버튼 14개 전부 제목·부제 2줄, 위 표와 문자열 일치
2. **1280×800 첫 화면에 버튼 7개가 다 들어오는지** (마지막 버튼 하단 px)
3. 카드 7장 제목·부제, `chip--field` 잔존 0
4. 캐러셀 6장 순환하며 `.cr-big`·`.cr-proj`·칩 동시 변경, `.cr-chip--field` 미표시
5. `니달리 / Suzi` 가 `index.html`에 0건 (`projects/` 안에는 남아 있어야 정상)
6. 1280 · 1000 · 375px 가로 스크롤 0 · 콘솔 0
7. `git diff --stat` 이 두 파일에만 걸치는지

---

## 3. 남은 작업 ② — 관리자 사이트 마커 반영 (배포 전 필수)

**맥에는 `~/Desktop/portfolio-admin` 이 있다.** (Windows PC에는 없어서 이번에 손대지 못했다.)

**마커가 13쌍 → 18쌍으로 늘었다. 관리자를 고치지 않고 '반영하기'를 누르면 새 마커 영역이 비거나 깨진다.**

| 변경 | 관리자 쪽 할 일 |
|---|---|
| `PROJECTS` 1쌍 → `PROJECTS_BUILT` / `PROJECTS_REVERSE` / `PROJECTS_LEVEL` 3쌍 | `renderers.ts` 의 프로젝트 렌더를 3영역으로 분리 |
| `HERO_STATS` 신설 | 요약 이력서 수치 4개 + 학력 줄 + 꼬리 줄 렌더 추가 |
| `HERO_BUTTONS` 신설 | `hero_button` 섹션 신설 — 필드는 FRONT_PAGE_PLAN §5-2 표 참조 (`title`·`href`·`kind`·`sub`·`status`·`role_group`·`engine_group`·`order`). `role_group`·`engine_group`으로 두 번 그룹핑해 2세트 출력 |
| `RESUME_EXTRA` 신설 | 대외활동·동아리 항목 렌더 추가 |
| 프로젝트 6개로 증가 | DB(또는 `backend/src/seed/seed-data.ts`)에 니달리 추가. **안 넣으면 반영 시 니달리가 사라진다** |

`sections.ts` 의 필드 정의와 시드도 같이 갱신할 것. 전체 사양은 **FRONT_PAGE_PLAN.md §5**.

⚠️ 이 저장소(`portfolio-admin`)는 **원격이 없다.** 최초 커밋 `2c68a03`. 전제: `colima start` 후 Docker Compose.

---

## 4. 남은 작업 ③ — 대외활동 12건 연월

이력서의 `Activity — Extracurricular` 패널은 이미 만들어져 있고 7행이 들어가 있다. 사용자가 노션 전문을 줬으므로 **날짜는 전부 확보된 상태**다. 추가 확인이 필요한 것만 남았다:

- ~~**펄어비스·카카오 수상 날짜** `23.11 ~ 24.03`~~ → **2026-07-28 사용자 확인 완료: 맞다. 그대로 둔다.**
- 노션에 있으나 사용자가 지정한 12건에 없어서 **뺀 것들**: 겜마루 겨울 아이디어톤 `24.12.22` · RESAT 브랜드마케팅 9기 `24.01~24.02` · 방배 ART 유스센터 축제 `24.03~24.05` · 뚝섬한강공원 벽화봉사 `24.05.04` · IT 동아 마케팅 기획 실무 프로젝트 `23.11~24.03`
- ~~넥슨 메커톤 · 컴투스 멘토링 스쿨은 12건 목록에 없다~~ → **2026-07-28 사용자 확인 완료: 의도한 것. 게임 활동 패널에만 두고 대외활동에는 넣지 않는다(이중 계상 방지). 꼬리 줄 "대외활동 12" 숫자도 유지.**

---

## 5. 검토 대기 (사용자 판단) — **2026-07-28 맥에서 전부 확정됨**

1. ~~캐러셀 카드 폭 272 → 236px 축소.~~ → **272px로 복원 확정.** `--crw` 되돌리고 `#showcase` 높이를 늘려 섹션 헤드와의 겹침을 피한다(헤드 하단 < 카드 상단 실측 필수)
2. ~~니달리 '물어뜯기' 환수종 분류.~~ → **환수종 스킬로 유지 확정.** 변경 없음
3. **모바일 375px에서 신규 이력서 패널 7행 중 3행이 2줄로 접힌다**(「내유」 전시 · 제일기획 45th · 군부대 UCC). 기존 게임 활동 패널도 같은 방식이라 회귀는 아님 — 그대로 둔다

---

## 6. 맥 환경에서 달라지는 것

이번 세션은 Windows에서 돌았다. 맥에서는:

| 항목 | Windows(이번) | 맥 |
|---|---|---|
| PDF 렌더 | poppler 없음 → **pymupdf(`fitz`)로 우회** | poppler 있으면 `pdftoppm` 사용 가능 |
| 이미지 리사이즈 | Pillow | `sips` (기존 파이프라인이 쓰던 것) |
| 관리자 사이트 | **없음** | `~/Desktop/portfolio-admin` 있음 |
| 셸 | PowerShell + Git Bash | zsh |

### 도구 한계 (맥에서도 그대로일 가능성 높음)

- **비상단 스크린샷이 검게 죽는다.** 페이지 결함이 아니라 캡처 파이프라인 결함이다. 레이아웃 검증은 `getBoundingClientRect` JS 계측으로 하고, 스크린샷은 상단 1장 육안 확인용으로만 쓸 것
- `left_click` 은 해시 앵커 이동을 일으키지 않고, `left_click_drag` 는 `pointerup` 을 전달하지 않는다 → JS 이벤트 디스패치로 검증
- 복구 순서: `preview_stop` → `preview_start` → `resize_window preset:desktop`

### 반복해서 밟은 함정

- **`img` 에 `width`/`height` 속성을 주면 CSS에 `height:auto` 를 반드시 함께 명시할 것.** 안 그러면 `aspect-ratio` 가 죽는다. 이 저장소에서 **세 번** 밟았다(리뷰 썸네일 · 프로필 사진 · 그 전)
- `.home a{color:var(--link)}` 가 카드 안 제목까지 앰버로 물들인다 → 새 카드 만들 때 제목에 `color:var(--text)` 명시
- 프로젝트 페이지 본문은 **의미 변경 금지**. 구조적 주입(셸)과 기계적 치환만 허용

---

## 7. 배포

```bash
git push origin main
```

1~2분 후 https://00ny.github.io 반영. **push는 사용자 환경에서만 가능**(세션에 GitHub 인증 없음 — 전례상 커밋까지만 하고 push는 사용자가 한다).
